//document.getElementById('textbox_id').value

function radioClick(type) {
   if (type === "default1") {
      //do default1 stuff
   } else if (type === "default2") {
      //do default2 stuff
   } else if (type === "custom") {
      //do custom stuff
   }
}